<% provide(:title, @submission.title) %>



<div class='container'>

  <%= render 'banner' %>

  <% if @submission.youtube %>
    <br><br>
    <div class="scroll-wrapper">
      <center>
        <iframe width="90%" height="550" src= "<%= @submission.url %>" frameborder="0" allowfullscreen scrolling="yes"></iframe>
      </center>
    </div>
  <% else %>
    <div class="scroll-wrapper">
      <div class='submission'>
    		<br><br><br>
        <iframe width="100%" height="2000" src="<%= @submission.url %>" frameborder="0" allowfullscreen scrolling="yes"></iframe>
      </div>
    </div>
  <% end %>
</div>
  
<!-- 
      <script>
        $(document).ready(function() {
            $(':submit').on('click', function(event) {
                event.preventDefault();

                var $button = $(this),
                    $form = $button.parents('form');

                var opts = $.extend({}, $button.data(), {
                    token: function(result) {
                        $form.append($('<input>').attr({ type: 'hidden', name: 'stripeToken', value: result.id })).submit();
                    }
                });

                StripeCheckout.open(opts);
            });
        });
        </script> -->
