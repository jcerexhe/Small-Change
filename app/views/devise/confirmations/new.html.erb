<div class="container">
  <div class="row">
    <h2 class="page-heading header-devise-tagline">Resend confirmation instructions</h2>
    <div class="col-xs-12 col-sm-6 col-sm-offset-3 col-md-4 col-md-offset-4">

      <%= simple_form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post }) do |f| %>
        <%= f.error_notification %>
        <%= f.full_error :confirmation_token %>

        <div class="form-inputs">
        <%= f.input_field :email, required: true, autofocus: true, label: false, placeholder: "Email", class: 'form-control input-lg main-input devise-input' %>
        </div>

        <div class="form-actions">
          <%= f.button :submit, "Resend confirmation instructions", class: 'btn btn-black btn-lg btn-block' %>
        </div>
      <% end %>
      <br>

      <%= render "devise/shared/links" %>

    </div>
  </div>
</div>
